from django.db import migrations

class Migration(migrations.Migration):
    dependencies = [
        ('bingo', '0002_systemconfig'),
    ]

    operations = [
        # Empty operations list - this just ensures Django knows the previous migration was applied
    ]
